/**
 * @function tKey Dummy function so i18next-scanner kan pick up from code. Enables exporting constants
 * @param key key to match in translation file
 * @param defaultValue value in default translation file
 */
export const tKey = (key: string, defaultValue: string): string => key || defaultValue;
